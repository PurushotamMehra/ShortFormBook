name: scrollable_book
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  epubx: ^4.0.0
  html: ^0.15.6
  file_picker: ^10.3.10
  path_provider: ^2.1.5
  path: ^1.9.1
  shared_preferences: ^2.5.4
  google_fonts: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/books/
